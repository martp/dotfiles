# GitHub CLI config
# Note: auth tokens are stored separately in hosts.yml (managed by gh auth login)
# and are NOT tracked in this dotfiles repo.
version: 1
git_protocol: ssh
editor:
prompt: enabled
prefer_editor_prompt: disabled
pager:
aliases:
    co: pr checkout
http_unix_socket:
browser:
color_labels: disabled
spinner: enabled
